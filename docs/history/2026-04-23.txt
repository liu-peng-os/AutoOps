AutoOps 阶段1底座建设更新记录

一、架构与规划
1. 更新独立演进路线文档，明确当前已进入阶段1
2. 重构阶段路线，调整为“方向与原则确认 -> 通用底座搭建 -> 接入型能力闭环 -> 模块化增强 -> 治理与优化”
3. 明确平台遵循“所有外部系统都作为可替换依赖处理”的原则
4. 明确新增能力方向：域名管理、运营工单、站点管理
5. 明确开发环境 PostgreSQL 版本采用 `PostgreSQL 16`
6. 回写开发环境 PostgreSQL 连接信息，便于内部协作联调
7. 新增当前阶段交接清单，明确交接前必须完成项、已知阻塞和下一位开发者推进顺序

二、后端底座
1. 新增 integration 接入骨架模块
2. 新增外部系统接入目录接口 `/api/v1/integration/catalog`
3. 配置结构新增 integrations 定义，支持声明外部系统清单
4. 预置首批模块目录信息：域名管理、运营工单、站点管理

三、安全修正
1. 移除鉴权失败时打印 JWT Secret 的调试日志

四、前端入口
1. 新增域名管理入口页
2. 新增运营工单入口页
3. 在资产管理下新增站点管理入口页
4. 新增统一外部接入占位页面，展示模块定位、平台原则和已配置外部系统
5. 左侧菜单增加阶段性引导入口，便于后续继续接入真实能力

五、当前说明
1. 本次先落“不会白做”的底座和入口骨架，不改现有业务模块核心逻辑
2. PostgreSQL 开发库暂未接入，后续接入后再继续推进 migration 和数据库实链验证
3. Go 侧全量校验受当前环境网络和依赖下载限制影响，未完成完整编译验证
